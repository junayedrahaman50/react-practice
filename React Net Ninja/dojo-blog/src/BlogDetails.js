const BlogDetails = () => {
  // lesson25: we're gonna return a simple template here for now, now we'll create a route for this blog inside App.js (App component) this is where our Routes reside.
  return (
    <div className="blog-details">
      <h2>Blog Details</h2>
    </div>
  );
};

export default BlogDetails;
